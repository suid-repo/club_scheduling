@model WebApplication.Core.Family

@{
    ViewBag.Title = "Delete";
}

<h2>@I18N.View.Family.Delete_Title</h2>

<h3>@I18N.View.Family.Delete_Subtitle</h3>
<div>
    <h4>@I18N.View.Family.Delete_Subtitle2</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="@I18N.GenericAction.delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
