@model DateTime?

@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("dd/MM/yyyy HH:mm") : string.Empty),
new { @class = "date form-control" })

<script type="text/javascript">
    $(document).ready(function () {
        $('.date')
            .datetimepicker({
                dateFormat: "dd/mm/yy",
                timeFormat: "HH:mm"
            });
    });
</script>


