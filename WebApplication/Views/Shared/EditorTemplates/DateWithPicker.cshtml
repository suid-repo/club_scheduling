@model DateTime?
    
@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("dd/mm/yy") : string.Empty),
new { @class = "date form-control" })

<script type="text/javascript">
    $(document).ready(function () {
        $('.date').datepicker();
    });
</script>

